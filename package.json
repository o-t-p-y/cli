{
  "name": "otpy-cli-workspace",
  "private": true,
  "type": "module",
  "description": "OTPy.ir public packages workspace — otpy (TypeScript SDK) and otpy-cli (init wizard).",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.23.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}
